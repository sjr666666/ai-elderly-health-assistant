<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.GuardianElderRelationMapper">

    <!-- 根据家属ID查询关联关系 -->
    <select id="findByGuardianId" resultType="com.example.backend.model.entity.GuardianElderRelation">
        SELECT * FROM elderly_medication.guardian_elder_relation
        WHERE guardian_id = #{guardianId}
          AND deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 根据老人ID查询关联关系（老人端查询自己的家属列表） -->
    <select id="findByElderId" resultType="com.example.backend.model.entity.GuardianElderRelation">
        SELECT * FROM elderly_medication.guardian_elder_relation
        WHERE elder_id = #{elderId}
          AND deleted = 0
        ORDER BY created_at DESC
    </select>

</mapper>
